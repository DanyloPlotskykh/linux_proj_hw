## linux_proj_hw


# Steps to configure the project:
1. autoreconf -i
2. ./configure
3. make
4. (optional) sudo make install (if you want to add the program to the PATH)

# To clean the project use:

```sudo make clean-all```